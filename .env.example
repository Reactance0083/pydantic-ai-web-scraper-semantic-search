# Anthropic API key (for pydantic-ai extractor and semantic ranker)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Path where the scraped index JSON is stored (optional, defaults to scraped_index.json)
INDEX_FILE=scraped_index.json
